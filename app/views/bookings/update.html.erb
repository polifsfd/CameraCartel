<h1>Booking Details</h1>

<p><strong>For Camera:</strong> Booking n-<%= @booking.id %> for <%= @camera.name %></p>
<p><strong>From User:</strong> <%= @booking.user.first_name %> <%= @booking.user.last_name %></p>

<% if @booking.status == true %>
  <%= link_to "Approve", booking_path(@booking)%>
  <%= link_to "Deny", booking_path(@booking.update), method: :put %>
<% end %>
