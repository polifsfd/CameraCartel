<h1>List of all categories' cameras</h1>

<ul>
<% @category.each do |category| %>
<li>
<%= link_to category.name, camera_path(category) %>
</li>
<% end %>
</ul>



<%= link_to "Go back to homepage", root_path, class: 'btn btn-primary' %>
